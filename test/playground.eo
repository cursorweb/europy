fn idxof(arr, num) {
    var i = 0;

    for itm in arr {
        if itm == num {
            break i;
        }

        i += 1;
    } else {
        -1
    }
}

var arr = [1, 2, 3, 4];
println(idxof(arr, 3));
println(idxof(arr, 7));