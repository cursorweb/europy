{
    // var a = "works";
    fn get_a() {
        println(a);
        /*    ^ a *should* be looking for the variable on the global side
                and bypass all the braces entirely! */
    }
    // var a = "shouldn't work";
    get_a();
}